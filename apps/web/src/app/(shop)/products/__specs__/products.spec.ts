import { describe, it, expect } from "vitest";

describe("Products Page", () => {
  it.todo("should render products grid");
  it.todo("should show empty state when no products");
  it.todo("should link each product to its detail page");
});
